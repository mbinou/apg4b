#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int N;
  cin >> N;

  vector<int> math(N);
  rep(i, N) {
    cin >> math.at(i);
  }

  cout << a << b << endl;
}

